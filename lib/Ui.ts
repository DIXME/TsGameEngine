import { KeyboardManager } from "./Keyboard";

export class ui {
    // this class will manage ui
    // the "ui" for now will just be an html overlay but
    // i might just make my own ui stuff from scartch idk

    constructor (keyboard: KeyboardManager){}
}